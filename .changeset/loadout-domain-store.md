---
"@cg3/equip": minor
---

Add a schema-versioned loadout domain store and `equip loadout` CLI commands for saving, listing, renaming, duplicating, deleting, and marking saved augment sets active without applying platform config changes.
